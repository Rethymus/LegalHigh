# LegalHigh 决策清单 —— 待决策项分析（2026-08-30）

> 背景：查漏补缺四轮（`开发计划-v3-查漏补缺与打磨.md`）后，登记册上剩余事项中有一部分不是「执行任务」而是「需要拍板的选择」。本文逐项给出选项、利弊与推荐，供决策。
> 决策原则
>
> ## ✅ 决议（2026-08-30，业主授权「结合真实调研结论直接决策；部署暂缓；非必要不再扰业主」）
>
> | # | 决议 | 执行状态 |
> |---|---|---|
> | 1 | 采纳选项 C：先移植大字模式 + 44pt 触摸目标 + safe-area（+16px 输入防 iOS 缩放）；BottomSheet/底部标签栏待真实移动反馈 | ✅ 已落地（设置→外观可开关；narrow 块已含 44pt/safe-area/16px） |
> | 2 | 采纳选项 B：不引入 jieba；/needs 展示层域词优先（有域词只显示域词），检索层 bigram 不动；jieba 再评估条件=金标证明检索受限 | ✅ 已落地（`curate_display` + `keywords_display` 字段 + 回归测试） |
> | 3 | 采纳选项 C：git 本地化 + pre-commit 快速门钩子；云端 CI 待业主确认托管策略 | ✅ 已落地（`.githooks/pre-commit` 四道快门；基线提交 96ac474） |
> | 4 | 采纳选项 C：双轨解读库（AI 起草 → 人工审核 → approved 对外）；首批 5 条 AI 草稿待审核，审核前不展示 | ✅ 管线落地（`article_explains.json` + `explains.py` + API + LawDetail 审核卡）；**待人工审核后前端即自动展示** |
> | 5 | 维持暂缓；条件触发 = D8 PIPL 通道 + M8-T1 备案材料完成后进内网部署准备 | ⏸ 按业主指示不作安排 |
> | 6 | 采纳选项 A：flk 比对 = 人工抽查 ≥10%（分层）+ 字段级全量对照，页面如实标注抽查口径；不爬取（红线7） | ✅ 文案统一（AppShell/LawsBrowse/Drafting/DataSources） |
>
（从项目本质导出）：①可信与可溯源优先于功能先进性；②人机协同双轨优先于全自动；③宁缺毋假、诚实降级；④尊重「非必要不决策」授权——优先可逆、零外部承诺的动作，把大决策转化为「条件触发」。

## 决策项 1：v6 窄屏形态的完成路径（登记册 C4 剩余）

**现状**：窄屏基线已落地（抽屉侧栏+单列栅格，390×844 全路由可用）。完整 v3 形态（底部标签栏 / BottomSheet / 44pt 触摸目标 / safe-area / 大字模式）完整存在于 `src-legacy-v1`，未移植 v6。

| 选项 | 利 | 弊 |
|---|---|---|
| A 完整移植 v3 形态 | 一次到位；真实移动体验；兑现 v3 规格 | 工作量 1–2 周；与 v6 桌面工作台壳层冲突需重做外壳；回归面大（37 路由×双模式×双形态） |
| B 声明桌面优先，维持基线 | 零成本；律师核心用户确实桌面为主 | 与「便民惠民」定位矛盾——普通人问法律问题多在手机上；违背 README 对大字模式（适老化）的既有承诺 |
| C 渐进增强：先移植高价值子集（大字模式 + 44pt 触摸目标 + safe-area），BottomSheet/底部标签栏缓行 | 成本≈完整移植 1/3；**大字模式是 v2 已有、v6 丢失的能力，属回归清偿而非新增范围**；触摸目标/safe-area 基本是纯 CSS；不动现有壳层 | 非完整移动体验；底部标签栏仍缺 |

**推荐 C**。理由：普通移动用户的高频场景是「搜索/需求解析/读法条」三个轻场景，最需要的是可读性（大字）与可点性（44pt），不是完整应用壳；BottomSheet 等交互重构应等真实移动用户反馈再决定，避免为想象中的用户重做壳层。触发条件：大字模式移植完成后，若出现真实移动流量/反馈，再评估 A 的剩余部分。

## 决策项 2：jieba 引入与关键词展示策略（登记册 D4 剩余）

**现状**：确定性降级路径的 bigram 分词有跨词噪声（「老板拖欠」→「板拖」）。检索层 BM25 用同一 tokenize，**噪声对检索无害**，仅 /needs 页展示层观感问题。

| 选项 | 利 | 弊 |
|---|---|---|
| A 引入 jieba（检索+展示全换） | 分词质量本质提升 | **风险与收益错位**：换分词器须重建 BM25 索引并重跑全部金标（评测基线漂移）；jieba 版本升级可能改变切分结果，动摇「确定性可复现分词」这一 harness 价值；新增依赖破坏最小依赖现状 |
| B 仅展示层治理：/needs 优先显示规则域词（词表质量高），生成关键词降噪显示（过滤与已显示词重叠的项、限制数量、保留「由分词程序生成」诚实标注）；检索层 bigram 不动 | 零依赖零风险；解决真实观感问题；检索可复现性完整保留 | 关键词集合本身仍含噪声（只是展示收敛）；域词未覆盖的查询仍可能露出二元组 |
| C 维持现状 | 零成本 | 普通用户看到「板拖」会怀疑系统能力——伤害「可信」观感，与项目本质冲突 |

**推荐 B**。理由：项目哲学是「模型只是引擎，harness 决定可靠性」——自研确定性分词的可复现性价值高于分词先进性；噪声的实际危害仅限展示层。jieba 的再评估条件：金标评测证明检索质量受分词瓶颈限制时（目前 40 组 hit@5=0.95，无此证据）。若未来引入，须锁定版本 + 全量金标回归 + 证据链记录。

## 决策项 3：版本管理与 CI 形态（登记册 C9 剩余）

**现状**：项目**不是 git 仓库**（无版本历史、无回滚能力）；质量门为本地一键 `run_qa.cmd` 五道。

| 选项 | 利 | 弊 |
|---|---|---|
| A git init + GitHub Actions 云端 CI | 每次变更强制过门；协作/回滚/审计 | **涉及数据外发决策**（代码托管到云端，业主「非必要不决策」授权范围待确认）；私仓费用/公仓披露问题；需 CI 适配（Chrome 路径已参数化，成本低） |
| B 本地脚本即止 | 零基建；数据不出本机 | 门靠会话自觉，无强制力 |
| C **先 git 本地化（含 .gitignore 审计：排除 .venv/node_modules/app.db/证据快照大文件）+ pre-push 本地门钩子；云端 CI 待业主确认托管策略后接**（脚本就绪，迁移成本低） | git 化本身是最基础的工程债清偿；本地钩子给门以强制力；不预设云托管决策，可逆 | 两步走 |

**推荐 C**。核心理由：一个以「append-only 审计、可复核」为立身卖点的项目，自己的代码却没有版本历史——这是与项目本质最相悖的工程债。git 本地化不外发任何数据、完全可逆，不属「必要决策」；云端才需要业主拍板。

## 决策项 4：逐条 AI 解释的内容路线（登记册 B6 剩余）

**现状**：法条详情页诚实标注「AI 通用阅读指引（非本条逐条解释）」，逐条解释未接入。

| 选项 | 利 | 弊 |
|---|---|---|
| A 接 LLM gate 直接生成逐条解释 | 1,953 条全覆盖；法条详情价值大增 | **gate 验不了「解释对不对」**：引用绑定只校验条号存在，无法校验释义正确；错误解释误导公众的风险高于无解释——触碰「内容生产者责任」红线；全量生成或实时的成本/延迟 |
| B 纯人工内容工程：优先高频条文（消保法25、劳动合同法19-20、民法典188 等常问 50–100 条）人工撰写通俗解读，带编写人/审核日期字段，其余维持通用指引 | 零幻觉；可溯源到编写者；80/20 覆盖高频需求 | 生产速度慢；需法律专业输入 |
| C **双轨复用：人工解读库为主体 + LLM 仅产「草稿建议」供人工审核采用（AI 起草 → 人工核验 → 上线），未审核不展示** | 与律师函「AI 起草+律师核验签发」同构——项目已验证的化解「能力 vs 责任」矛盾的正确结构；既不虚构能力也不放弃效率 | 建设解读库 schema（author/date/reviewer/source_note）与审核流程有一次性成本 |

**推荐 C**。理由：这是把项目签名模式（双轨签发）复用到内容工程；新解读库走既有证据纪律（server/data/ + 前端禁止手写），与「宁缺毋假」完全一致。

## 决策项 5：部署时点与形态（M8-T4，业主「非必要不决策」适用）

**现状**：部署暂缓。规划方向为「第一阶段律所/法务内网 docker-compose；公网前完成备案复核」。

| 选项 | 利 | 弊 |
|---|---|---|
| A 内网 docker-compose 先行 | 合规风险最低（不面向公众则备案义务未触发）；真实反馈；工作量小 | 需内网环境与种子用户；社会价值延迟兑现 |
| B 公网直接开放原型 | 立即兑现便民价值 | **违反红线 1**（先备案/登记再对公众开放）——不可行，排除 |
| C 继续暂缓 | 零风险 | M8 其余任务（备案材料、隐私通道）失去牵引；项目停在原型 |

**推荐「条件触发的 A」**：维持暂缓，但当 **D8（PIPL 删除/导出通道）与 M8-T1 备案材料清单完成**时自动进入内网部署准备——这两项本就是上线 checklist 签核项（M8 验收 gate ③），先做完再部署零返工；同时把「要不要部署」的开放决策转化为「条件是否满足」的事实判断，最贴合非必要不决策授权。

## 决策项 6：flk 逐条比对的执行方式（M6-T1 前置）

**现状**：flk.npc.gov.cn 无开放 API；语料 1,953 条「flk 比对待办」在 UI 如实标注；pcl-2023 末条疑点待复核。

| 选项 | 利 | 弊 |
|---|---|---|
| A 人工抽查比对（如 10% 分层抽样：每部法律首末条+高频引用条+随机条）+ 字段级全量对照（条数/施行日期/机关，来自 manifest）+ 页面如实显示「抽查口径+日期」 | 合规（无爬取）；诚实（比对覆盖透明）；与风险登记册既定缓解一致 | 「逐条比对」目标缩水为「抽查+字段全量」 |
| B 对 flk 检索接口半自动逐条查询 | 名义上完成逐条 | **触碰红线 7（禁止未授权爬取）**——自动化访问公开网站仍可能违反服务条款，需法务评审且大概率不可行 |
| C 等待官方开放数据/API | 零风险 | 期限不可控，比对无限期搁置 |

**推荐 A**，并把「逐条比对完成」的验收口径修订为「抽查 ≥10% 无差异 + 字段级全量一致 + 差异清单公开」。理由：诚实降级原则同样适用于对自家完成度的表述——「如实标注抽查口径」比「宣称逐条但无法证明」更符合项目本质。

## 已决策、无需再议（防止反复）

- **LLM 默认状态**：默认关闭、BYO key、仅已备案提供方（M6-T4 已落地）——开启与否由使用者决定，项目侧不预置。
- **判例数据分层**：L1 精选内置 / L2 CAIL 统计 / L3 联邦检索（`数据取舍与分层架构决策-2026-08-30.md` 已定）。
- **裁判文书网批量抓取**：不做（红线 7）。
- **Word 修订双轨路线**：python-docx XML 级最小修订子集（w:ins/w:del）+ WPS/Office 双端回归（M 计划风险登记册已定）。
- **UTC 存储本地展示**、**检索单一引擎**、**派生数字禁硬编码**：第四轮起为硬规则。


---

# 决策清单 v2 —— 当前开放决策项（2026-08-30 第七轮后）

> 上半部分 6 项已全部决议并执行（见上方决议表）；以下为当前**仍然开放**的决策项。
> 与上一批不同：这一批多数不是「二选一的方案取舍」，而是「上线前必须确定的人/物/时点」——推荐均已给出，但共同特点是**依赖业主指定人或授权数据**，AI 不可代行。

## 决策项 7：大模型提供方选型（M8-T1 材料 A1，LLM 实评测的前置）

**背景**：harness 已就绪（`ai_providers.json` 8 家 OpenAI 协议兼容目录 + 三道 gate + `llm_eval.py` 抽样评测器），选型本身只差「定哪家 + 给密钥」。

| 选项 | 利 | 弊 |
|---|---|---|
| A DeepSeek（deepseek-chat） | 已完成生成式AI备案；OpenAI 协议官方兼容；成本最低；中文法律文本表现有口碑 | 通用模型，法律垂直深度不及专业法律产品；备案号需随提供方政策核实索取 |
| B 智谱 GLM / Kimi / 通义 | 同为国产已备案阵营，协议兼容 | 同 A，差异主要在价格与限流 |
| C 法律垂直模型（如法行宝类） | 领域微调 | 公开接口/备案情况需逐一核实，接入选定后仍走同一 gate |
| D 本地 Ollama/vLLM 部署开源模型 | 数据不出域；零 API 成本 | 需自建推理资源；**模型本身的备案问题不因本地部署消失** |

**推荐 A（DeepSeek 先行）**，理由：①备案状态相对明确（仍以索取备案证明为准）；②base_url 直接进现有目录零改造；③`llm_eval.py` 五十题抽样门给了低成本试错通道——不达标换下一家，**选型错误可逆**。硬条件不变：**无备案证明不接入**（办法 §17）。**✅ 决议（2026-08-30 第八轮）：采纳「任意 OpenAI 协议端点」路径——目录 8 家+custom 均兼容，不锁厂商。**✅ 实评测完成（2026-08-31）：智谱 GLM（glm-4-flash）50 题抽样门 PASS（有效回答句级引用覆盖 100%、红线 0、premise 纠错 5/5；23 题被引用绑定 gate 扣发=按设计工作），报告 docs/qa-evidence/llm_eval_2026-08-31.json。任意已备案厂商按同门即可接入。**

## 决策项 8：代码托管与云端 CI 激活（决策 3 后半，唯一悬而未决的「数据外发」）

| 选项 | 利 | 弊 |
|---|---|---|
| A GitHub 私仓 | 生态最全，Actions 直接跑 `.github/workflows/qa.yml` | 代码出境（美国托管）；依赖 GH 可达性 |
| B Gitee / 自建 GitLab（境内） | 数据主权；Gitee Go/自建 Runner 可跑同五门 | 自建需运维；Gitee CI 能力弱于 Actions |
| C 维持本地（pre-commit 门已存在） | 数据零外发 | 无远端备份；门靠提交者自觉（钩子可被 `--no-verify` 绕过） |

**推荐 B（境内托管）若业主允许外发**：法律项目对数据主权敏感，境内平台更协调；五门脚本均平台无关（pytest/node 命令），迁移成本≈0。**维持 C 亦完全可接受**——本地 pre-commit 已实跑生效，这是「非必要不决策」的默认态。触发条件：业主明确托管平台并建仓，随后把 qa.yml 的 runner 适配提交即可。

## 决策项 9：最小用户体系引入时点（内网部署前置，审核担责字段的真实性根基）

**背景**：原型无用户体系（演示账号）。解读审核人、律师核验人（verify role）、批注复核人目前均为**自报字符串**——内网多人使用时，「谁审核的」无法核实。

| 选项 | 利 | 弊 |
|---|---|---|
| A 内网阶段引入最小本地账号（用户名+口令，绑定真实姓名/角色；审核与核验动作强制登录人） | 担责字段从「自报」变「认证」；审计可信度质变；M7-T3 的前置 | 一次性开发（登录+会话+角色三件事）；口令管理责任 |
| B 维持自报字符串（ prototype 口径） | 零成本 | 内网多人场景下审计可信度为「谁都可以说是李律师」 |
| C 对接律所现有 AD/LDAP | 复用既有身份 | 依赖具体律所环境，排期不可控 |

**推荐 A**，放在**内网部署准备期**执行（与 docker-compose 同批），排入 M7-T3 前置。C 作为 A 之后的增强路径。判定标准：只要存在 ≥2 名真实使用者，「自报」即不成立。 **✅ 最小可行版已执行（2026-08-30 第八轮）**：「工作身份档案（本机）」上线——设置→Account 维护姓名/角色（localStorage），审核人/核验人/复核人默认取此身份；Drafting 核验/签发如实传角色（非执业律师会被 server gate 正确拒绝）。完整认证账号仍按 A 于内网期执行。

## 决策项 10：解读库的生产与审核分工（B6 管线的内容运营侧）

**背景**：审核工作流已闭环（工作台一键审核），5 条 AI 草稿待审。但「谁来审核、按什么节奏」未定。

| 选项 | 利 | 弊 |
|---|---|---|
| A 业主指定 1 名法律专业人员任固定审核人（建议节奏：每周一批 20 条 AI 草稿，优先高频条文） | 责任单一明确；产能稳定 | 单点依赖 |
| B 双人复核（起草→初审→终审） | 质量更高 | 成本×2，原型期过重 |
| C 开放众包（多名律师自愿审核） | 规模快 | 责任链模糊；与「审核人担责」字段冲突 |

**推荐 A 起步**，审核人姓名即写入每条解读的 reviewer 字段并随法条页公示——这既是质量把关也是**审核人的专业署名展示**（激励相容）。触发条件：业主指人；未指人前 5 条草稿维持待审（页面诚实显示「未接入」，不影响其他功能）。

## 决策项 11：「真实移动反馈」的采集机制（决策 1 的触发条件落地）

**背景**：决策 1 约定「BottomSheet/底部标签栏待真实移动反馈再评估」——但反馈从哪来未定义。

| 选项 | 利 | 弊 |
|---|---|---|
| A 埋点统计移动端访问/操作 | 数据客观 | 与隐私最小化原则紧张；原型期数据量小无统计意义 |
| B 复用投诉通道：窄屏页脚加「移动端体验反馈」快捷入口，落库+审计 | 零新基建（/api/complaints 现成）；定性反馈对 1–2 周期内的小样本更有用 | 非量化 |
| C 等内网种子用户口头反馈 | 零成本 | 无沉淀 |

**推荐 B**：一条 complain 分类（kind=mobile）+ 窄屏媒体查询下的反馈快捷链接，改动极小；收集到 ≥10 条移动反馈或内网部署时，即满足决策 1 的再评估触发。**✅ 已执行（第八轮）**。

## 决策项 12：LLM 功能的开放范围（A1 选型并过抽样门之后）

| 选项 | 利 | 弊 |
|---|---|---|
| A 全站开放 AI 草稿（问答页/审查页/起草页都挂） | 功能声量最大 | gate 验内容合规但验不了「解释质量」，暴露面大 |
| B 仅研究页「AI 结论草稿」+ 解读库「AI 起草」两个内部点位（现状路径） | 每一处 AI 输出都有人工 gate 兜底；暴露面最小 | 功能可见度低 |
| C 分级开放：先 B，跑满 50 题抽样门 + 两周零投诉后再评估扩展 | 数据驱动的渐进 | 节奏慢 |

**推荐 C**。这与办法 §9 内容生产者责任、项目「工作流而非聊天」原则同构：AI 永远在人工 gate 之后逐级放量。**✅ 决议（2026-08-30 第八轮）：采纳 C。现状即 B 态（研究页 AI 结论草稿 + 解读库 AI 起草）；扩展条件=抽样门全过 + 两周零投诉。**

## 决策项 13：金标持续扩容节奏（M6-T5 已达 100，之后呢）

| 选项 | 利 | 弊 |
|---|---|---|
| A 一次性扩到 200+ | 覆盖广 | 纯人力堆叠，边际收益递减（hit@5 已 0.98） |
| B **事件驱动扩容**：每次语料扩张/规则改动/真实 badcase 出现时新增对应金标（当前已实行） | 金标跟着风险走；每次改动都有回归价值 | 无固定数字目标 |
| C 停止扩容 | — | 语料扩张时评测盲区扩大 |

**推荐 B 固化为制度**（写入 AGENTS 约定）：「语料/检索/分词规则变更 → 必须先加对应金标再改」，配合词法鸿沟观察（本轮 14 条首拟未命中实录）作为语义检索再评估的数据积累。**✅ 已固化（第八轮）**。

## 决策项 14：合规中心评测看板的内容边界（M7-T4）

**背景**：评测看板已实时计算（/api/evals）。待决：`llm_eval` 报告（含 AI 输出样本）是否公示。

| 选项 | 利 | 弊 |
|---|---|---|
| A 全公示（含抽样原文） | 透明度最高 | AI 输出原文公示=内容生产者责任自担范围扩大 |
| B 只公示指标数字 + 报告文件名（可申请查阅） | 责任可控，透明度可验证 | 弱一档 |
| C 不公示 | — | 违背「只公开可证之事」的对称义务 |

**推荐 B**：数字可证、原文可控；随 M7-T4 一并实现（评审面板已有基建）。**✅ 决议（2026-08-30 第八轮）：采纳 B。llm_eval 报告落 docs/qa-evidence/（本地可查），对外仅公示指标数字；M7-T4 实现时执行。**

## 附：上一批 6 项决议的执行结果确认（证据指针）

| 决策 | 结果 | 证据 |
|---|---|---|
| 1 窄屏渐进增强 | ✅ 大字/44pt/safe-area/16px 已上线；BottomSheet 缓行 | 决策清单决议表；截图 28–33 |
| 2 jieba | ✅ 不引入；展示层域词优先（keywords_display） | test_needs 回归；决策项2 注记 |
| 3 git+CI | ✅ git 本地化+pre-commit 四门；云端待决策项 8 | 提交历史 96ac474 起；.githooks/ |
| 4 解读双轨 | ✅ 管线+审核工作流闭环；5 条草稿待人工（决策项 10） | explains.py/工作台审核 Tab |
| 5 部署 | ⏸ 暂缓；触发条件 D8✅+M8-T1（清单已交付，待 A/B 表落实） | docs/compliance/备案材料清单 |
| 6 flk 抽查 | ✅ 口径落地；机器侧 0 问题；人工环节待执行（旧接口已下线的实证记录） | corpus_selfcheck 报告；手册〇节 |


---

# 决策清单终态 —— 待业主输入清单（2026-08-31 更新）

> 决策项 1–14 已全部处置：11 项执行/决议完毕（证据见上表与各轮登记册），3 项**必须由业主提供资源或指定人**，属「AI/项目侧不可代行」，列示如下。每项均已把项目侧准备做到「输入到位即可零改造推进」。

## 输入 1：模型密钥（解锁决策 7 执行）

- 需要什么：任一已备案提供方的 API Key（推荐 DeepSeek，决议 7A）——设置环境变量 `DEEPSEEK_API_KEY` 即可。
- 输入到位后自动发生：`scripts/llm_eval.py` 50 题抽样门实跑 → 全过则研究页「AI 结论草稿」按决策 12 分级开放。
- 利：LLM 实评测闭环、解读起草提质。弊：API 费用（50 题评测预计 <10 元量级）。不提供的后果：LLM 相关功能维持关闭，检索式能力不受影响。

## 输入 2：解读审核人（解锁决策 10 与 B6 最后一步）

- 需要什么：指定 1 名法律专业人员（姓名将公示于每条解读的「人工审核」字段并担责）。
- 输入到位后自动发生：工作台「解读审核」Tab 逐条点击审核 → 10 条 AI 草稿排队自动上线。
- 利：解读库内容真正面向公众、审核人获专业署名展示。弊：每周约 1–2 小时审核投入。不指定的后果：解读维持「未接入」诚实态，其余功能不受影响。

## 输入 3：代码托管平台（解锁决策 8 与云端 CI）

- 需要什么：确认托管平台并建仓（GitHub 私仓 / Gitee / 自建 GitLab 三选一），`.github/workflows/qa.yml` 与五门脚本均已就绪。
- 输入到位后自动发生：推送即激活云端五门；本地 pre-commit 照常。
- 利：远端备份+强制门。弊：代码外发（境内平台可缓解）。不提供的后果：本地门已实跑，工程纪律不受影响。


---

# 决策项 15：官方解读来源策略（2026-08-31 立项并执行）

**背景**：AI 起草的逐条解读受律师双轨 gate 约束，但「官方解读」（司法解释、法工委答问等）权威性更高且不受律师审核约束——需要决策官方来源的接入策略。

| 方案 | 利 | 弊 |
|---|---|---|
| A AI 逐条解读 + 免责声明 | 产能快 | 免责声明不转移《生成式AI办法》§9 内容生产者责任；与「严格证据」产品定位冲突 |
| B 官方解读优先（司法解释/法工委答问链接） | 权威性最高（裁判依据级）；司法性质文件不受著作权限制；与引用不变量完美兼容 | 覆盖密度取决于官方发布节奏 |
| C AI 起草 + 律师双轨（仅官方无解读的条文） | 填补 A/B 的空白 | 与 A 相同的成本 |

**✅ 决议（2026-08-31 第十五轮执行）：三层优先级体系 = B > C > A。**

- B（最高优先）：司法解释作为新语料类型接入 build_corpus 管线（htjs-2023 69 条 + wlxf-2022 20 条），通过 article_links.json 建立法条→解释条文的映射，法条页「关联司法解释」tab 展示映射卡片。
- C（补充）：官方无解读的条文，AI 草稿走律师双轨审核（工作台审核面就绪，10 条待人工）。
- A（最后）：仅当业主明确接受「AI 标识+免责」形态且满足三条约束（标识办法义务/仅复述条文文义/免责声明）时才可上线。当前默认不启用。
- 金标扩展：+6 组覆盖 htjs-2023/wlxf-2022/civl-2020:965，106 组合计 hit@5=0.97≥0.90。


---

# 决策记录（2026-09-15，业主授权「授权！」）

## 决策 16：桌面签名与自动更新 → **采纳 A（不签名，GitHub Releases 自助下载）**

- 业主于 2026-09-15 对待决事项总体授权（「授权！」）；按 v5 推荐路径 A 落地：
  桌面安装器不进入 Release 附件，桌面候选包验收与签名采购（B 路径）等真实用户规模再议。
- 触发 B 的条件不变：真实用户规模出现，或业主给出签名预算。

## 决策 24：案例库扩张 → **采纳 A（事件驱动）**

- 当前 15 件（指导案例 12 + 外国 3）五域谱系视为稳态；
  后续仅随真实 badcase / 新指导案例发布逐件采集（同决策 13 金标节奏），不设批量滚动排期。

## 同轮解锁：flk 人工抽查（转正条件 1 的定案部分）

- 业主授权下于 2026-09-15 执行 flk 抽查首批（只读、人类节奏）：
  **pcl-2023 末条定案**（官方结构树 306 条、末条=第三百零六条；保留附则说成立，残留说否定），
  psm-2025 元数据核对一致。证据与限制见
  `docs/qa-evidence/flk-spotcheck-2026-09-15-定案与结构核数.md`。
- 文本级 ≥10% 逐条比对余量（约 165 条工作单）续核：官方正文为 OFD 版式，
  无文本接口，逐条文本比对依赖人工阅读器目验（方法与限制已登记）。
- 解读审核人闸门不变：AI 不自审，须业主指派人类具名。
