# 已登记白区（不静默）

按 FLERF 审计（2026-09-16）登记。白区=已知未实现、有明确实现路径、系统在未实现前 fail-closed（拒答/标注）而非错答：

1. **AS_OF_DATE 时间检索**——✅ fail-closed 形态已实现（R144，`app/temporal.py`）：时间指涉检测 + 逐命中 `in_force_at_as_of` 标记 + 显式告知块；`/api/qa/ask` 与 `/api/search` 均支持显式 `as_of`。**时点文本对照已全量落地（R167 切片 → R168 全量采集 → R169 as_of 命中面收口）**：全部 37 份非现行历史版本全文结构化入 `data/law_versions_fulltext/`（双向过 selfcheck/pytest）；`GET /api/laws/{id}/versions/{vid}/fulltext` + LawDetail 版本 Tab 对照查阅；**as_of 命中时答案卡/检索命中自动携带 `historical_version`**（适用版本=施行日与公布日均 ≤ as_of 中公布日最新者；同条号对照文本+移位风险随行标注；适用即现行时不出字段）。**剩余白区**：跨版本条号重编号映射与历史文本独立检索索引——**重编号映射已落地（R170，`app/version_renumber.py` + `GET /api/laws/{id}/renumber-map`）**：相邻版本（按公布日升序）三级确定性对齐（同键身份+text_changed 标记 → 文本全同移位 → difflib 阈值 0.85 模糊移位；子条号参与键），两侧 unmatched 如实报告（废止/新增）；实测刑法 1979→1997 全文改写 ch=192、1997→2020 修正 ch=147+新增 53（253之一在 unmatched_to）与修法史实互证。**剩余**：历史文本独立检索索引（现状=现行检索命中+as_of 同条号/映射对照）；**映射已接入 historical_for_card（R171）**：同条号未命中或命中但映射显示内容系重编号而来时，反查相邻版本对定位祖先条号并返回其历史文本，`located_via/mapped_from_no/mapped_ratio` 字段与「经重编号映射定位」移位标注随行；身份映射不覆盖同条号结果。**独立检索索引已落地（R172，`app/history_index.py` + `GET /api/history/search`）**：37 份历史全文 4,688 条独立 BM25 命中（同一引擎同一分词、独立命名空间——历史条文永不混入现行检索排名），law_id/version_id 过滤与非现行口径随行；现行主检索契约经测试钉住不变；**前端消费入口已双落（R173 LawDetail 版本 Tab 本法范围框 + R174 DataSources 全站检索卡）**，judge 视觉复核 pass。**剩余**：无（索引已可被站点级与本法级两种粒度消费）。
2. **claim 级 NLI 校验**——🟡 确定性中间步已落地（R168，gate3 升级）：逐句逐分句引用绑定 + 词面重合（≥0.20）+ 确定性裁判承诺识别之外，新增**数值一致性核验**——断言分句中的数值（三十日/6个月/二倍/3年…，CN 与阿拉伯归一化、条文引用第X条不按事实数值抽取）必须能在被引原文中找到同值同单位，否则扣留全文（「编造数字」是词面重合抓不住的高频幻觉，此为高精度确定性代理；既有 42 项 gate 测试零回归）。**剩余白区**：全句级语义 NLI 需引入模型——继续 benchmark-gated（须凭 ADR + 评测数据引入，不静默加依赖）。
3. **统一 A0–D 权威枚举**——分层事实存在（grade/source_kind/level/authority_class），统一命名待法律专家审查（报告 §18 自述「不应由工程师规定法律效力」）。
4. **运行时 On-demand Fetcher**——原型全快照制、无运行时外呼；Source Registry 已为未来 Fetcher 备好合规门（approved 才可运行）。
5. **自动 Source Drift canary**——✅ 自动化落地（R164）+ CI 接线（R168）+ 文档级指纹（R170 `--deep N`）+ 逐「条」级指纹（R173 `--deep-articles`）+ **首/末条抽样位（R180 `--article-positions ends`）**：`middle`=中位条（默认）、`ends`=首条+末条（解析器最易吞并/粘连的边界区——R45 零宽字符教训的在线化）；ends 实跑 8/8 OK（行政诉讼法1989 首#1末#75、行政复议法1999 首#1末#43）。**剩余白区**：cron 定时调度挂部署决策（决策清单）。
6. **Citator 产品层**（被引用于/负面历史检查/后续案例）——🟡 种子已落地（R165，FLERF §26）：`app/citator.py` + `GET /api/laws/{law_id}/cited-by`——已核实案例 research_refs 对本法/本条的**精确引用反查**（子条号参与匹配，253之一≠253），权威分级统计（指导性案例/域外判例）；LawDetail「关联案例」Tab 从 ±40 邻近启发式升级为精确匹配、「引用关系」Tab 从 Neo4j 占位升级为 Citator 概览（被引统计+高引条文 chips+负面历史诚实声明）。**剩余白区**：负面历史检查（后续案例/修法如何对待本条）无数据源，显式声明不提供；全国裁判文书层面引用全景不可得（不爬取）；案例-法条引用语料规模化后建图谱。
7. **案例按字段加权检索**——✅ 全链闭环（R146 API + R150 前端）：`app/cases.py` 字段权重三档（balanced/facts/reasoning），facts 偏向零化 holding/result（纯 Facts↔Facts）、reasoning 偏向 Holding×4+Result×3（「为什么这么判」）；`/api/cases` 增 bias 参数；8 项钉住测试（偏向改变排名/召回扩到 facts 字段/案号精确保底/确定性）；前端 CaseSearch 分段控件（综合/类似案情/裁判理由）+ 一句话口径说明，选档即触发重查（api.listCases 携带 bias）。
8. **Evidence Ledger 完整形态**——✅ 全链路覆盖 + §19 强类型 + 独立账本表 + source_native_id（R164 建账 → R172 全链路 → R174 收口 → R180 native id 升级 → R181 robots 定案 → R184 AI 生成链路收口）：**独立 `evidence_ledger` 表**（append-only，无更新/删除通道，与状态变更审计 audit_log 分离）；**六条链路**（qa/研究备忘录/合同审查/要件分析/文书起草/**AI 生成（ai_chat，实体=provider/model，blocked 扣留同样留痕）**）统一写入 **§19 VerifiedEvidence 强类型**（pydantic extra=forbid）：canonical_document_id（law@生效日）/canonical_unit_id（子条号参与）/content_hash（条文精确文本 sha256）/**document_hash（证据快照文档级 SHA-256）**/**source_native_id（终态见下——81/108 部映射，未核验法诚实留空）**/source_id（host→注册表来源，未注册 host fail-closed 拒绝）/authority_class/location/published_at/fetched_at/verification 四项核验状态；`GET /api/evidence`（管理门）只读查询、PIPL 导出包含账本。**案件分析红线尊重方式**：模块无状态纪律不破——实体=随机 uuid（绝不取案情文本哈希）、零用户材料痕迹（测试钉住）；起草链路钉住「用户填写字段不入账」。**qa 链路数值核验的设计判断（R180）**：qa 答案卡=命中的法条原文（非生成断言文本），无「编造数字」面，gate3 数值一致性不适用——该核验只属于生成文本链路（AI 草稿）。**source_native_id 终态（R181）**：flk robots.txt 明文「禁止使用任何自动化工具、脚本、爬虫程序采集或复制网站数据」（Disallow: /）——自动化核验路径被法律红线（LEGAL-006 + 站点明文声明）封死，覆盖止步于**既有 82 份人工核验记录的精确映射 81/108 部**（`data/flk_native_ids.json`；严格精确相等匹配补齐 3 部：学前教育法/爱国主义教育法/法治宣传教育法；子串误配 3 例已剔除——社会保险法≠保险法等）。**余 27 部需人工在浏览器检索后提供 bbbs 或 detail JSON**（人工浏览不受 robots 约束）。R181 补充：flk 搜索接口 `POST /law-search/search/list` 持续返回 500「系统异常」（bundle 含 captchaImage——搜索面受验证码保护），程序化搜索路径同样封死；`GET /law-search/search/flfgDetails?bbbs=` 单条查询接口仍可用但需先知 bbbs。**结论：余 27 部的 flk native id 只能通过人工浏览器操作获得**——业主可在 flk 网页搜索目标法条名后，从浏览器开发者工具 Network 面板读取 flfgDetails 请求中的 bbbs 参数提供给本项目，映射即可补齐至 108/108。**剩余白区**：无（§19 全字段有着落；flk 余 27 部为人工输入型）。
9. **needs 取证词面误配 badcase**——✅ 按「金标先行→A/B→ADR」收口（R166，ADR-0005）：5 组劳动法域金标（541 组）量化基线（新组 1/5，raw 层 hit@5 0.9612）→ 新增 `labor-termination` 受控主题组（组查询=目标条文规范词面）+ 组级 `when` 条件试用期组（命中即优先供位）→ A/B 定案：编排层（qa/needs/search 产品路径）新组 **1/5→5/5**、全量 hit@5 0.9593→0.9612、MRR 0.7787→0.7791，零回退超限；**raw 层词面地板如实保留**（4 组问法 raw 仍 MISS——门=引擎地板、编排=产品层，两层口径分离，不粉饰）；6 项钉住测试（test_labor_termination_retrieval.py，ADR 名次锚）防漂移。

白区不阻塞发布：每一项都有「未实现即不冒充」的 fail-closed 形态兜底。
