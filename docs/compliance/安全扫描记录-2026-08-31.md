# 安全扫描记录（M8-T2·清零高危目标）

## 扫描执行（2026-08-31）

- 工具：Mimosa 深度扫描（sealed，`security_scan` MCP，`static_only_no_runtime_execution` 边界）
- 项目：`F:\ZCodeData\LegalHigh`；Scan ID：`scan-2026-08-31T03-28-24.208Z-45fe010a20ea`
- 封印：`sha256:c783e7f58555a644ef2d7cf1c1ad3ec82bf0cce8389cf21cbffba9fff0cddc9f`（seal.json）
- 原始报告：`C:\Users\92813\.mimosa\security-scans\project-7f8f043531096767d2c9103a\...\report.md`（工具目录，sealed）

## 结果

| 等级 | 数量 | 明细 |
|---|---|---|
| 高危 | **0** | —（M8-T2「清零高危」达成） |
| 中危 | 0 | — |
| 低危 | 1 | `llm_eval.py:49` 不安全随机数（CWE-330） |

**唯一低危的处置：豁免并说明**。该处 `random.seed(20260830)` 为评测抽样**可复现性**的有意设计（金标 100 组中抽 50 题，须同种子可重放比对），不用于任何安全用途（令牌/密钥/会话）。豁免理由与替代方案评估：改用 `secrets` 将丧失可复现性、违背评测纪律；安全敏感场景全库检索确认无 `random` 使用（`grep -rn "random\." server/app web/src`，仅评测脚本）。

## 诚实边界（扫描方声明的限制，原文转述）

- 运行状态 **inconclusive**：部分分析阶段未能完整覆盖；调用图部分不完整（动态派发/规模限制），跨文件可达性可能不完整。
- 报告为 sealed JSON 的投影，**非运行时验证**。

结论：静态深度扫描视角下高危清零；正式上线前（决策 5 触发时）应再跑一次当期版本扫描 + 补充运行时验证（final_verify + 五道门），三方证据合并方为完整安全面。
